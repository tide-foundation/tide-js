node_modules/
dist/
fixtures.json
# Never commit real per-user secrets — the *-users.json glob covers
# load-test-users.json and any future per-environment variants.
# (The committed sample lives at load-test-users-sample.json which
# does NOT match this glob.)
*-users.json
!*-users-sample.json
*.log
.env
# Load-test output (R22). Contains per-iteration latency JSONL and the
# trailing summary block — operator-owned, may carry test-environment
# userIds, never commit.
loadtest-results*.json
loadtest-results*.jsonl
